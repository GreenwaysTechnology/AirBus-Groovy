import com.airbus.oo.groovybeans.Person

def person = new Person()
//access variables without assigning any values
println("id ${person.id} name ${person.name} status ${person.status} rank ${person.rank}")